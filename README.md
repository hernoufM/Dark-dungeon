# Installation

This project use **stack** build system.

```bash
curl -sSL https://get.haskellstack.org/ | sh
```

# Use 
```bash
# Compile
stack build
# Run
stack run
# Tests (Hspec, QuickCheck)
stack test 
```